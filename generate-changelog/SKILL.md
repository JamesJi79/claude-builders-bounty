---
name: generate-changelog
description: Parses git history and generates structured CHANGELOG.md
tags: [changelog, git, release]
---

# Generate CHANGELOG

Parses `git log` and produces `CHANGELOG.md` grouped by Conventional Commits types.

## Usage
```bash
claude generate-changelog
# or:
bash generate-changelog/changelog.sh
```